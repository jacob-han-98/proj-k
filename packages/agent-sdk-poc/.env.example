# Claude Agent SDK (Bedrock)
CLAUDE_CODE_USE_BEDROCK=1
AWS_BEARER_TOKEN_BEDROCK=your-token-here
AWS_REGION=us-east-1

# Anthropic API (scripts/build_summaries.py Haiku 호출용)
# Bedrock으로 Haiku를 쓰려면 ANTHROPIC_BEDROCK=1 로 추가 설정
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Server
PORT=8090
