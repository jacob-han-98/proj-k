.venv/
.env
__pycache__/
*.pyc

# 런타임 로그 (재생성 가능)
index/_build_summaries.log.json

# pair comparison 실행 결과 (세션별, 커밋 금지)
tests/compare_out/

# KG는 심볼릭 링크로 관리 — 로컬 환경별로 생성
# index/knowledge_graph.json 은 symlink, 리포지토리 밖에 실데이터 있으므로 건너뜀
